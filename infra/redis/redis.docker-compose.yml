services:
  redis:
    image: redis:7.4.2-alpine
    restart: always
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    ports:
      - "6379:6379"
    volumes:
      - ./redisdata:/data